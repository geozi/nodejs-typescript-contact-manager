/**
 * Common constants.
 * @module src/domain/constants/common.constant
 */

/**
 * Contains numeric constants that are used across project files.
 *
 * @type {object}
 * @property {number} MONGODB_ID_LENGTH - The length of an ObjectId in MongoDB.
 */
export const commonConstants = {
  /**
   * The length of an ObjectId in MongoDB.
   * @type {number}
   */
  MONGODB_ID_LENGTH: 24,
};
